## Why

<!-- 描述本次变更的原因。为什么现在需要做这件事？ -->

## Scope

<!-- 受影响的页面和功能有哪些？ -->

## BCF（核心业务路径）

<!-- 识别并列出受影响页面的"核心业务路径 (BCF)"。
每个 BCF 必须包含从入口到出口的完整跳转闭环。
如果本次变更不涉及业务逻辑（如纯样式调整），可跳过此节。 -->

| BCF | 入口 | 关键步骤 | 出口 | 验收标准 |
|-----|------|---------|------|---------|
| BCF-1 | <!-- 入口页 --> | <!-- 步骤1 -> 步骤2 -> ... --> | <!-- 出口页 --> | <!-- 如何验证成功 --> |

## Not Scope

<!-- 明确排除的范围。本次变更不包含什么？ -->

## Acceptance

<!-- 必须全部满足才能视为完成 -->

- [ ] 所有 BCF 路径已验证
- [ ] 无占位组件（每个控件有真实 handler）
- [ ] UI / executor / workflow JSON 正确消费了关键数据字段
- [ ] 代码可正常编译（无 typecheck 错误）
