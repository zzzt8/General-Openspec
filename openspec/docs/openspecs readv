我仔细看了一轮。结论先说：

你的 General-Openspec 现在最大问题不是“功能不够”，而是“它想在 OpenSpec 上再造一层更重的 OpenSpec”，导致命令体系、skill 体系、CLI 体系之间出现了多处错位。

它有很多好想法：断点续传、layer、增量验证、test attribution、plan、多 change 编排。但目前有几个会直接导致 Cursor/Agent 绕圈、误执行、甚至命令循环的问题。

一、我看到的项目结构

你的仓库定位是“OpenSpec Skill 系统的可移植版本”，README 里说明它包含 .cursor/commands、.cursor/skills、openspec/schemas、openspec/specs 和 openspec/config.yaml，用于复制到任意项目作为 spec-driven 开发框架。

核心结构大概是：

General-Openspec/
├── .cursor/
│   ├── commands/
│   │   ├── opsx-onboard.md
│   │   ├── opsx-sync.md
│   │   ├── opsx-explore.md
│   │   ├── opsx-propose.md
│   │   ├── opsx-review.md
│   │   ├── opsx-apply.md
│   │   ├── opsx-verify.md
│   │   ├── opsx-archive.md
│   │   ├── opsx-debug.md
│   │   ├── opsx-plan.md
│   │   ├── opsx-continue.md
│   │   ├── opsx-skip.md
│   │   └── opsx-sync-specs.md
│   └── skills/
│       ├── _shared/
│       ├── openspec-onboard/
│       ├── openspec-sync/
│       ├── openspec-sync-specs/
│       ├── openspec-explore/
│       ├── openspec-propose/
│       ├── openspec-review/
│       ├── openspec-apply/
│       ├── openspec-continue/
│       ├── openspec-skip/
│       ├── openspec-verify/
│       ├── openspec-archive/
│       ├── openspec-debug/
│       ├── openspec-plan/
│       ├── openspec-skill/
│       └── ga-*
└── openspec/
    ├── docs/
    ├── schemas/
    ├── specs/
    └── config.yaml

官方 OpenSpec 当前的 Quick Path 是 /opsx:propose → /opsx:apply → /opsx:sync → /opsx:archive，Expanded Workflow 才包含 /opsx:new、/opsx:continue、/opsx:ff、/opsx:verify、/opsx:bulk-archive、/opsx:onboard。

二、最严重的问题
1. 命令命名和官方 OpenSpec 不一致：你用 -，官方用 :

你的 README 和 command 文件使用的是：

/opsx-propose
/opsx-apply
/opsx-verify
/opsx-archive

官方文档当前统一使用的是：

/opsx:propose
/opsx:apply
/opsx:sync
/opsx:archive

官方命令参考里明确列的是 /opsx:propose、/opsx:explore、/opsx:apply、/opsx:sync、/opsx:archive。

这个问题不只是风格问题。它会导致：

你自己的文档：/opsx-apply
官方生成的命令：/opsx:apply
Cursor 里可能同时出现两套入口
用户不知道该用哪一套
Agent 可能读到官方文档后切换到冒号命令

建议：

统一改成官方命名：

/opsx:propose
/opsx:apply
/opsx:verify
/opsx:archive

旧的 /opsx-apply 可以作为兼容 alias，但不要作为主入口。

2. .cursor/commands 太薄，可能不会真正触发 skill 逻辑

你的 opsx-apply.md 只有几行：

# OpenSpec Apply

Implement OpenSpec change tasks. Supports resume from checkpoint, incremental verification, dependency scheduling.

See `.cursor/skills/openspec-apply/SKILL.md` for full implementation.

也就是说 command 文件只是“指向 skill 文件”，并没有把核心执行协议写进去。

这在 Cursor 里很危险。因为你不能完全假设 Agent 会自动去读取 .cursor/skills/openspec-apply/SKILL.md。它可能只读 command stub，然后按自己的理解执行。

这会造成你之前遇到的现象：

模型知道有个 apply
但不知道 apply 的完整约束
于是它凭感觉做
或者开始反复解释“我需要读取 skill”

建议：

command 文件至少要写成：

# /opsx:apply

Before doing anything:
1. Read `.cursor/skills/openspec-apply/SKILL.md`.
2. Follow that skill as the source of truth.
3. Do not implement from this stub alone.

Then execute the user's apply request.

更稳的是：把核心 10 条规则直接内嵌到 command，skill 只作为扩展文档。

3. openspec-apply 和 openspec-continue 存在循环委托风险

openspec-apply 写的是：

/opsx-apply（无参数） → 续传模式 → 委托 openspec-continue

同时 openspec-continue 又写：

续传执行完全委托给 openspec-apply

也就是说：

apply 无参数 → continue
continue → apply
apply 无参数 → continue
...

这就是一个典型的路由闭环。

这会直接导致 Agent 绕圈：

我将委托 continue
continue 要委托 apply
apply 又判断无参数要委托 continue

建议：

只能保留一个执行引擎。

我建议这样定：

/opsx:apply = 唯一执行引擎
/opsx:continue = 只负责定位 active change + 找断点，然后调用 apply with explicit change-name + task-id

也就是：

/opsx:continue
→ 找到 change-name
→ 找到 next task
→ 调用 /opsx:apply <change-name> --from <task-id>

不要让 /opsx:apply 无参数再反向委托 /opsx:continue。

4. openspec-continue 里把 /opsx-apply 也写成了 alias，和 apply skill 冲突

openspec-apply 的 alias 是 /opsx-apply。
但 openspec-continue 里也把 /opsx-apply 写成了 alias。

这是严重冲突。

结果可能是：

用户输入 /opsx-apply
系统不知道该匹配 openspec-apply 还是 openspec-continue

建议：

立刻删掉 openspec-continue 里的 /opsx-apply alias。

只保留：

aliases:
  - /opsx:continue
5. 你把官方 CLI 的职责和自定义 skill 的职责混在一起了

官方 CLI 明确提供了一批 agent-compatible JSON 命令，例如：

openspec list --json
openspec show --json
openspec validate --all --json
openspec status --json
openspec instructions --json
openspec templates --json
openspec schemas --json

这些就是给 AI/脚本用的结构化接口。

你的 skills 里却大量写了自己的状态解析、task 元数据解析、layer 解析、验证解析、测试归因解析。比如 apply 里自己规定读取 tasks.md checkbox、解析 opsx-meta、计算 affected layers、执行增量验证、再 full validate。

问题是：

官方 CLI 已经提供 status / instructions / validate
你的 skill 又自己造状态系统
两套状态源会不一致

尤其是你现在还引入：

tasks.md checkbox
tasks-state.json
opsx-meta
config.yaml layers
change-index.md
review.md
test-design.md

这会让 OpenSpec 的“轻量 spec layer”变成一套重型流程系统。

建议：

原则改成：

官方 CLI 能给的状态，不自己解析
官方 schema 能表达的 artifact，不用 skill 硬编码
skill 只负责调用顺序和失败处理
三、架构层面的错位
6. 你的 workflow 变得比官方更“phase-locked”

官方 Workflows 文档强调的是：

Actions, not phases
Dependencies are enablers, not gates

也就是说命令是动作，不是僵硬阶段；依赖是告诉你什么可做，不是强制你线性过关。

但你的 General-Openspec 现在有很多硬关卡：

Schema Preflight 硬关卡
Artifact Precondition 硬关卡
Test Failure Attribution 硬关卡
related / undetermined 阻断
apply 前必须检查 dependencies
archive 前必须 verify

这些对于企业级流程不是错，但它已经偏离了官方 OpenSpec 的“fluid not rigid”。官方 README 也强调 OpenSpec 的理念是 fluid、iterative、easy、built for brownfield，而不是复杂流程。

建议：

不要默认开启这么多硬关卡。应该分 profile：

core-light       默认，贴近官方
strict-review    高风险项目
enterprise       全硬关卡
meta-plan        多 change 编排

你现在是把 enterprise 模式塞给所有项目。

7. verify 的定位和官方不一致

官方命令文档里 /opsx:verify 的定位是：

检查 completeness / correctness / coherence
报告 CRITICAL / WARNING / SUGGESTION
不阻止 archive，只暴露问题

官方文档明确写了 verify “does not block archive, but surfaces issues”。

你的 openspec-verify 却写：

valid: false → 禁止 archive
invalid > 0 → 禁止 archive

你的 archive 里还设计了 verify 失败时的 force-archive 分支。

这会让用户困惑：

官方：verify 是辅助检查
你的系统：verify 是准入门禁

建议：

如果你想保留强验证，命名上要区分：

/opsx:verify       官方语义：检查，不修复，不强阻断
/opsx:gate         你的语义：发布/归档硬门禁

或者在 config 里加：

opsx:
  archive:
    require_verify: false

默认 false，严格模式才 true。

8. config.yaml 被塞入了非官方配置语义

官方 customization 文档里，openspec/config.yaml 的主要用途是：

schema
context
rules

官方还说明 schema resolution order 是 CLI flag → change metadata → project config → default。

你的 config 里额外塞了：

layers:
verify:
openspec:
meta_rules:

并且这些字段被 skill 当成执行系统的核心。

这不是一定错，因为 OpenSpec 支持自定义 schema 和项目配置。但问题是你没有把这些字段放进一个明确 namespace。

现在是：

layers:
verify:
openspec:
rules:
meta_rules:

容易和官方未来字段冲突。

建议：

改成：

schema: spec-driven

context: |
  ...

rules:
  proposal:
    - ...

opsx:
  layers:
    priority:
      - engine
      - backend
      - editor
      - runtime
      - ui-skin
      - meta
    paths:
      engine:
        - packages/engine/src/

  verify:
    default:
      typecheck: pnpm typecheck
      test: pnpm test

  gates:
    schema_preflight: true
    archive_requires_verify: false
    auto_commit: false

把你自己的东西全放进 opsx:。

四、执行稳定性问题
9. openspec-apply 内容过重，已经不是 skill，而是执行器设计文档

你的 apply skill 有 386 行、13.4KB。
里面包含：

状态真相源
入口路由
Health Check PowerShell
Schema Preflight Bash
Artifact Precondition
git diff 增量验证
layer 计算
task 元数据解析
依赖检查 JS 伪代码
Test Failure Attribution
三维打分制
大 task 拆分
Guardrails

这会导致模型：

读完以后注意力分散
把示例脚本当真实脚本
把规则解释一遍而不是执行
在 shell / PowerShell / JS 之间混乱

你之前问“skills 过长会导致什么问题”，这份就是典型案例。

建议：

拆成：

openspec-apply/SKILL.md              只保留 80~120 行执行协议
_shared/HEALTH.md                    健康检查说明
_shared/SCHEMA-PREFLIGHT.md          schema 硬检查
_shared/VERIFY.md                    增量验证逻辑
_shared/TEST-ATTRIBUTION.md          测试归因
_shared/TASK-PARSER.md               checkbox / opsx-meta 解析规范

skill 里只写：

Run schema preflight via _shared/SCHEMA-PREFLIGHT.md
Run incremental verification via _shared/VERIFY.md
When tests fail, use _shared/TEST-ATTRIBUTION.md
10. apply 里强制每个 task 后 Git commit，风险很高

openspec-apply 单 task 步骤里写了：

执行 task 内容
Git commit
Git diff 获取实际文件
执行增量验证
更新 checkbox

这个很危险。

原因：

用户工作区可能已有未提交改动
AI 可能把用户改动一起 commit
commit message 没定义
Cursor agent 不一定应该直接 commit
很多项目不希望每个 task 一个 commit

建议：

默认不要 auto-commit。

改成：

opsx:
  git:
    auto_commit: false

apply 逻辑改为：

优先 git diff --name-only 识别当前改动
如果 auto_commit=true，且 working tree 干净，才允许 task commit
11. git diff HEAD~1 依赖 commit 历史，续传时容易失真

debug 和 continue 里都用到了类似：

git diff --name-only HEAD~1
git diff --name-only HEAD~3 HEAD

如果用户没 commit、squash 过、或者 task 不是一 commit 一步，这些结果就不可信。

建议：

改成：

git diff --name-only
git diff --cached --name-only

或者在 task 开始前记录 baseline：

OPSX_TASK_BASELINE=$(git rev-parse HEAD)

然后 task 后比较。

12. apply 里的 layer 列表写死

apply 里计算 affected layers 时写死：

for layer in engine backend editor runtime ui-skin

但你的 README 又说所有配置从 openspec/config.yaml 读取。

这就是冲突：

嘴上说项目无关
实际写死 Prism/前端项目的 layer

建议：

config 改成：

opsx:
  layers:
    priority:
      - engine
      - backend
      - editor
      - runtime
      - ui-skin
      - meta

skill 只读取 priority，不写死。

五、和官方 OpenSpec 的版本/模式冲突
13. 项目内版本标记混乱

我看到几套版本同时存在：

README：General Openspec v0.1
.cursor README：v5.3
config context：OpenSpec v4.0
GENERATE-CONFIG：v5.1
官方 OpenSpec release：v1.3.1

你的 README 说安装 @fission-ai/openspec@latest，官方 README 当前 release 是 v1.3.1。
但你的 config context 还写 “OpenSpec v4.0”。

这会造成用户完全分不清：

OpenSpec CLI 版本是多少？
General-Openspec 版本是多少？
Skill system 版本是多少？
Schema 版本是多少？

建议：

统一版本语义：

OpenSpec CLI compatibility: >=1.3.1
General-Openspec version: 0.1.0
Skill schema version: 5.3
Config generator version: 5.1

并在 VERSION 或 README 顶部写清楚。

14. 官方 openspec update 会重新生成 AI guidance，你的手写 .cursor 可能被覆盖或漂移

官方 CLI 文档写得很清楚：升级 CLI 后运行 openspec update 会重新生成 AI tool configuration files。

你的项目又要求用户复制整套 .cursor 到目标项目。

冲突点：

用户复制你的 .cursor
再运行 openspec update
官方可能覆盖/更新 .cursor/skills 和 commands
你的自定义技能可能丢失、冲突或漂移

建议：

不要直接把整个 .cursor 当长期源文件。

更合理的方式：

General-Openspec 作为 schema bundle / overlay
官方 openspec init/update 生成基础命令
你的脚本再 patch 自定义 opsx 扩展

或者明确要求：

openspec update 后必须运行：
node .cursor/skills/_shared/GENERATE-INDEX.js
node .cursor/skills/_shared/GA-PATCH.js
六、OpenSpec 官方哲学层面的偏差
15. 你把 review、test-design、plan 做得太重

官方默认 core 只有：

propose
explore
apply
sync
archive

Expanded workflow 才有 verify/onboard/continue 等。

你的系统默认暴露了 13 个命令 + 15 个 skill + 5 个 GA skill。

这对你个人研究很有价值，但对“可移植版本”不友好。

大多数项目只需要：

/opsx:propose
/opsx:apply
/opsx:archive
/opsx:explore

其余都应该是高级模式。

建议：

README 分成三档：

Core：propose / explore / apply / archive
Strict：review / verify / debug / sync-specs
Meta：plan / skill / ga-*

不要让新用户一上来面对 18 个技能。